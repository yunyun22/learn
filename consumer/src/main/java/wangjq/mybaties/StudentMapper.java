package wangjq.mybaties;

import org.apache.ibatis.annotations.Select;

public interface StudentMapper {
    /**
     *
     *
     * @param id
     * @return
     */
//    @Select("SELECT * FROM students WHERE id = #{id}")
//    Students selectStudents(String id);
}
