<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="wangjq.mybaties.StudentMapper">
    <select id="selectStudents" resultType="wangjq.mybaties.Students">
        select sname from students where sno = #{id}
    </select>
</mapper>